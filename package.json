{
  "name": "payment-validation-tests",
  "version": "1.0.0",
  "description": "Автотесты для валидации формы платежа",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0"
  }
}
